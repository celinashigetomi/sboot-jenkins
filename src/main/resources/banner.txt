-------------------------------------------------------------------------------
  Aplicacao: ${application.title}
Spring Boot: ${spring-boot.version}
      Porta: ${server.port}
     Perfil: ${spring.profiles.active}
   Contexto: ${server.servlet.context-path}
-------------------------------------------------------------------------------